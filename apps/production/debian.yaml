apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: debian
  namespace: flux-system
spec:
  interval: 10m
  path: apps/general/debian
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
